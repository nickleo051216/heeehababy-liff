# HEEEHABABY 買家前台（LINE LIFF）

- `index.html`：前台本體（單檔）。`CFG.mock=true` 為示範模式，不連任何後端。
- `vercel.json`：禁止搜尋引擎收錄等安全標頭。
- 正式模式需設定 `CFG.liffId` 與 `CFG.base`，並關閉 `CFG.mock`。

API 規格見 japan-system 的 BUYER_API_CONTRACT.md。
